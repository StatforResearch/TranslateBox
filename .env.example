# Production DNS name pointing to your server; Caddy obtains HTTPS automatically.
SITE_ADDRESS=translate.example.com
CORS_ORIGINS=https://translate.example.com
OPENAI_API_KEY=
# Generate with: python3 -c 'import secrets; print(secrets.token_urlsafe(32))'
# This is an operator access code, NOT an OpenAI key. Minimum 32 characters.
OPERATOR_TOKEN=
OPENAI_REALTIME_MODEL=gpt-realtime-translate
MAX_LISTENERS=30
